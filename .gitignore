# Secrets and credentials
*.pem
*.key
*.p12
*.pfx
.env
.env.*
!.env.example
secrets.env
env.local
roditwallet.env
.vault-token
.npmrc
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
id_dedalo*

# Host-local sibling dir if someone clones into the wrong place
infra-app/

# Shell / editor noise
.bashrc
.bash_history
.bash_logout
.profile
.selected_editor
.sudo_as_admin_successful
.wget-hsts
.cloud-locale-test.skip
.gitconfig
.lesshst

# Legacy path: scan outputs now live under ~/infra-app/trivy-scan-results/
trivy-scan-results/
